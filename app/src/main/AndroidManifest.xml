<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.g.forgetmenot">

    <application
        android:allowBackup="true"
        android:debuggable="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/logo_flower_01"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomePage" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SpeechTakeActivity" />
        <activity android:name=".TakeActivity" />
        <activity android:name=".TakeSuccess" />
        <activity android:name=".TakeFailure" />
        <activity android:name=".SpeechPutActivity" />
        <activity android:name=".SpeechPutIsIt"></activity>
    </application>

</manifest>